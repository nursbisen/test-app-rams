export const FPS = 30; // Frames per second

export const FIRST_FRAME = 0;
export const LAST_FRAME = 119;
export const DEFAULT_FRAME = 18;
export const FRAMES_COUNT = 120;

export const FRAMES_TO_VIEW = [18, 49, 86];

export const ALL_FRAMES_ARRAY = Array.from(Array(FRAMES_COUNT).keys());
